# Server
server.port=8080

# PostgreSQL Connection Pool (HikariCP)
db.url=jdbc:postgresql://localhost:5435/ledger_db
db.user=ledger_user
db.password=ledger_password
db.pool.size=10

# Redis Configuration
redis.host=localhost
redis.port=6379
redis.idempotency.ttl.seconds=3600