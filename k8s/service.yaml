apiVersion: v1
kind: Service
metadata:
  name: ledger-app-service
  labels:
    app: ledger-app
spec:
  type: NodePort
  selector:
    app: ledger-app
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080